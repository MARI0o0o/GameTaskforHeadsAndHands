package com.example.gametaskforheadsandhands.presentation

import android.app.Application
import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import com.example.gametaskforheadsandhands.domain.entities.Hero

class SkillsHeroModelFactory() : ViewModelProvider.Factory {
//    override fun <T : ViewModel> create(modelClass: Class<T>): T {
//        if (modelClass.isAssignableFrom(SkillsHeroViewModel::class.java))
//            return SkillsHeroViewModel(hero) as T
//        if (modelClass.isAssignableFrom(GameViewModel::class.java))
//            return GameViewModel(hero) as T
//        throw RuntimeException("Unknown view model class $modelClass")
//    }
}